const express = require('express'); const router = express.Router(); const msgController = require('../controllers/messageController'); const auth = require('../middlewares/auth'); router.get('/', msgController.getMessages); router.post('/', auth, msgController.createMessage); router.put('/:id', auth, msgController.updateMessage); router.delete('/:id', auth, msgController.deleteMessage); module.exports = router;